[project]
name = "pysiesta"
version = "0.1.0"
description = "Siesta 💤 — give an idea, answer a few questions, come back to working code. Autonomous dev pipeline with local or remote LLMs."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Jairo Rodriguez Arias" }, { name = "Luigi Palumbo" }]
keywords = ["ai", "agents", "ollama", "llm", "pipeline", "flet"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: X11 Applications",
    "Intended Audience :: Developers",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Code Generators",
]
dependencies = [
    "flet>=0.28.3",
]

[project.urls]
Homepage = "https://github.com/paluigi-moltis/siesta"
Repository = "https://github.com/paluigi-moltis/siesta"

[project.scripts]
pysiesta = "siesta.gui.app:main"
pysiesta-cli = "siesta.pipeline.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/siesta"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
src = ["src", "tests"]
# pragmatic set: correctness + imports + pyupgrade; the inherited upstream
# codebase intentionally uses blind excepts / unchecked subprocess calls in
# guards and tests — not worth churning 200 passing tests over.
[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
[tool.ruff.lint.per-file-ignores]
# inherited prose-heavy code (prompts, markers, URLs) — cosmetic only
"src/siesta/pipeline/phases.py" = ["E501"]
"src/siesta/pipeline/learn.py" = ["E501"]
"tests/*" = ["E501"]
